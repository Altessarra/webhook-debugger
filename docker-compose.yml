services:
  webhook-debugger:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATA_DIR: /app/data
    volumes:
      - webhook_data:/app/data
    restart: unless-stopped

volumes:
  webhook_data:
